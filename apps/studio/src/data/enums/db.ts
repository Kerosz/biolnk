export enum UserStatus {
  BASIC = "basic",
  VERIFIED = "verified",
}

export enum PageLinkPreference {
  PATH = "path",
  SUBDOMAIN = "subdomain",
  CUSTOM = "custom",
}

export enum LinkKind {
  DEFAULT = "default",
  ICON = "icon",
}
