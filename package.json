{
  "name": "biolnk",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev --parallel --no-cache",
    "lint": "turbo run lint",
    "format": "prettier --write '**/*.{ts,tsx,.md}'",
    "deploy:studio": "turbo run build --scope=\"@biolnk/studio\" --includeDependencies --no-deps",
    "deploy:web": "turbo run build --scope=\"@biolnk/web\" --includeDependencies --no-deps"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "turbo": {
    "baseBranch": "origin/main",
    "pipeline": {
      "build": {
        "dependsOn": [
          "^build"
        ],
        "outputs": [
          "dist/**",
          ".next/**"
        ]
      },
      "lint": {
        "outputs": []
      },
      "dev": {
        "cache": true
      }
    }
  }
}